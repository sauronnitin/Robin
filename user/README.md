# Keep this folder for local onboarding outputs (gitignored contents).
# After onboarding you should see profile.json and your resume here.
#
# Knowledge Graph (after clone + first save):
#   user/kg/individual.json  - your Individual graph (created on POST /api/kg/individual)
#   user/kg/share_prefs.json - opt-in for anonymous All aggregate (later)
# Defaults/dummies ship in dashboard/kg/ until you save.

