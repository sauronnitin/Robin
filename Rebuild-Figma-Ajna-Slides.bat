@echo off
echo DEPRECATED: This flattens slides into one image per frame. You cannot edit text or layout.
echo.
echo Use figma-inject-plugin instead (separate editable layers + screenshot frames only).
echo   docs\case-study-jobhunter\figma-inject-plugin\manifest.json
pause
