## Learned User Preferences

- LLM routing as of 2026-08-12 (do not revert to the 2026-08-10 Groq-primary claim below -- that was superseded the very next day and this bullet used to still assert it, which is exactly the kind of stale-doc drift that caused real confusion this session): `job_fit_analyst`, `resume_tailor`, `cover_letter_writer`, `content_humanizer_ai_detection_specialist`, `latex_resume_compiler_drive_publisher`, and `human_like_application_specialist` (the main-loop apply agent -- easy to miss since its tool is Playwright, not an LLM-content task, but it's on the same `_gemini_flash` instance) run on Gemini Flash (`gemini/gemini-2.5-flash`) as PRIMARY -- switched in `aac5484` because Groq 70B's 12K TPM ceiling proved unreliable across a multi-job run. `_gemini_flash.fallback_llm = _groq_70b` as of `c6c9f68` (2026-08-12): Gemini itself has no other fallback behind it, so without this a bare Gemini 503/empty-choices response used to crash the whole crew process (it did, live, run `b8866b0f71df`). `_groq_70b`/`_gemini_flash` are now each other's `fallback_llm`; a `_fallback_depth` kwarg caps that at exactly one hop so a double failure can't ping-pong forever (`tests/test_llm_fallback.py`). LinkedIn-equivalent agents (`linkedin_job_fit_analyst`, `linkedin_resume_tailor`, `linkedin_cover_letter_writer`) are `groq/llama-3.3-70b-versatile` with no fallback wired -- a deliberate, separate routing, not an oversight; do not "fix" them to match the main pipeline. Tool/mechanical agents (Scout and others on `_groq_8b`) stay on `groq/llama-3.1-8b-instant` with Gemini Flash as fallback. Never Gemini Pro.
- Prefer existing workspace Google Auth or Drive credentials over asking the user to hunt for keys; when Drive is configured, save pipeline/agent outputs into the JobHunter Google Drive project folder, not only under local project paths.
- For the agents pipeline live preview, keep the UI compact and mostly visual with no scrolling; favor a node-based dashboard layout; tokens panel uses abbreviated counts (e.g. 41000 -> 41k) with padding so columns do not clump.
- Whenever a feature or backend capability is added (new agent, task, tool, route, setting), also ship the matching dashboard GUI in the same change: regenerate `dashboard/pipeline-data.js` via `python dashboard/_gen_pipeline_data.py`, wire canvas edges/cards, expose controls or status in the UI, and verify the card/control is visible after refresh. Backend-only drops without a GUI surface are incomplete. When trying designs or making major UI changes, never replace the live mock/canvas by default: snapshot into `dashboard-archive/vNN-short-slug/` (`python dashboard-archive/snapshot.py`, include `mockup.html`) and append a `VERSIONS.md` entry (see `dashboard-archive/save-iteration.md`) so case-study demos stay available. Build the experiment as a new archive version and/or a separate mock route. Only overwrite live `dashboard/` if the user explicitly asks to ship or replace it. Live shell is Steep (light default, Dark toggle via `jh-steep-theme` localStorage); canvas uses the same theme tokens. Applicant Dashboard (`#s-dashboard`) uses apple-bento-grid layout with Steep / SPEC §5 colors only (no Apple neon). Do not invent metric or benchmark numbers. ATS Lift stays blocked when `ats_after` is NULL (never show 0). Sample-gate rates (suppress below 10 real applications, hide below 4). Exclude `dry_run` from funnel, rates, and time-saved (keep dry-run in quality/fit). Funnel reads `application_event`, not `application.status`.
- At the start of any JobHunter session (and before claiming a pipeline/dashboard task is done): read `dashboard/errors/latest.json` (or `GET http://localhost:5959/api/errors/latest` if the server is up). If `ok: false` and `open.length > 0`, fix those issues first. Never ask the user to paste Activity errors when that file exists. During live runs, keep watching that bus: treat a user-initiated Stop as intentional (thinking or screen recording); on automatic failure or stop, diagnose and fix without waiting for the user. Pause must set `user_paused` and stop LLM calls (no token drain); resume only on Play/unpause. While paused, show Play only on the paused agent card (title-row top-right), same action as section resume/retry. Pipeline break/retry modals need Dismiss so the user can edit the canvas, then Play to resume.
- In the dashboard model picker: list provider models with connection status (active / inactive / disconnected); do not unilaterally prune models the user might choose; remove discontinued models only; show Load only when disconnected; keep menu item text fully visible inside the card; preselect agent-context recommended models and mark them blue; support a Swap control between primary and fallback. Agent cards need a separate Fallback models field (`fallback_llm`) listing lower-tier options from the same Model catalog, ordered as next-best picks; changing Fallback must not overwrite primary Model (`llm`). Live runs follow the current primary after Swap; AutoFix may promote `fallback_llm` into primary on transient LLM errors before retry.
- Prefer USA-first job search (then Canada/EMEA), product/UX/UI design roles across all boards, and last-24-hour postings as high priority; defer Oceania, Dubai, Singapore, South Korea, Brazil, and Japan.
- Keep LinkedIn applications in a separate disconnected canvas loop (not the main Scout→Log chain); support both Easy Apply and external Apply; flag JD AI/bot-check jobs for user edit before applying. Default Apply is all boards except LinkedIn; LinkedIn testing and builds live in the mockup left-sidebar **LinkedIn Lab** tab (`#s-linkedin`), not in the main Apply/Dashboard queue. **Knowledge Graph** (Individual) is a mockup left-sidebar page (`#s-knowledge`), never a canvas Activity-rail tab; it covers uploaded and inferred material for that person. All KG is a separate admin surface later (opt-in anonymized aggregate across users); share copy must be privacy-first (improve the product, not monetize). KG page is a vertical split: left for upload/sources/intelligence, right for the interactive graph. First visit runs an LLM onboarding interview as a centered overlay over the dimmed split-pane (not a cramped sidebar chat strip): Typeform-style, one question visible at a time (do not stack or grow height for the next), each with MCQ answers and a dictation button inside the answer field; document upload on the first step; short JTBD persona routing (`climb` / `pivot` / `urgent` / `explore` plus urgency) before the backbone questions; infer from uploaded docs then ask additive questions; goal is career uplift via adjacent skills/roles and higher pay. Resume/doc upload is optional mid-flow (not a gate); uploaded docs remain removable and update the graph; persist analysis so it reloads on click without re-upload (same for Profile resume). Optional Market Pulse (SerpAPI Google News) is off by default, cached, and fetches only on Analyze when enabled (never on page load). Graph nodes keep always-visible labels (not hover-only) and reasonably sized spheres. Career reality-check goal: auto-suggest Primary/Secondary with Confirm (O*NET-backed scoring when available), surface gaps/stats and curated USA salary-band estimates plus user current/target pay, and open a Career Intelligence brief on node click (Analyze deepens). Never auto-switch mockup tabs when background work finishes (job scan, refresh, and similar); stay on the page the user selected.
- ATS form fill default is direct Playwright using the Simplify-style field map (profile + `user/apply_autofill.json` + optional `APPLICANT_*` env); Simplify extension is fallback only when required fields remain empty; harvest Simplify-filled values back into `apply_autofill.json` for later direct runs. Never invent years/experience, work auth, salary, or EEO answers.
- Resume parse (onboarding upload or later Profile resume upload) must fill Profile experience, education, languages (default English when missing), and skills (section headings like Design are categories, not skill names); experience schema is `{company, title, location, startDate, endDate, isCurrent, dates, bullets[]}` with newest-first sort and month pickers in Profile; PDF text extraction must preserve spaces across bold/italic font changes and must not leak the next job header into the previous bullet; Profile has a top-right collapsible resume preview (default collapsed) that renders the original uploaded file inline (PDFs via blob or a `.pdf` route with internal scroll; do not rebuild as a redesigned HTML sheet); form toolbar uses Save Profile instead of a Preview CTA (resume-row Preview may remain); never name parser tech brands (Open Resume, pdf.js, etc.) in user-facing UI or API responses (AGPL attribution stays internal under `tools/` only); Gemini Flash optional enrich only; Profile progress must be able to reach 100% when those sections are reasonably complete (no hard cap below 100). Profile/Settings share Steep field primitives (`.st-field-wrap` / `.st-field-label` / `.st-field`): sentence-case serif labels above inputs (not under-field captions), nested list rows omit redundant type labels such as Bullet above the input, roomier icon padding, rounded fields proportional to the card, and stack/bento layouts instead of long thin single-row fields; Application Questions stay display-only for salary/EEO (editable ATS defaults live under Settings > Apply autofill).
- For the canvas: Figma-like sections (group/ungroup Ctrl+Shift+S/U, movable/resizable with visible backgrounds, Sim/Start/Stop/Pause on a right-edge floating card not header chrome, Done/Stage/Flags/Tokens/Elapsed top-right, pan over cards inside sections, drag-out keeps card data but drops edges, multi-select moves together, cards fully in or out of sections on refresh never clipped mid-frame); bottom-left chat stays project-aware and action-capable (execute supported dashboard controls, not only describe them); LinkedIn section ends with minimal live browser preview (pan/zoom, no Explain AI chrome); confirm token cost before LLM-backed preview narrate. Mockup Ask Cursor is a collapsible right rail (not a left-sidebar page) that talks to the Cursor IDE agent (not Gemini) and accepts image/video refs; keep canvas `#chatDock` on Gemini Flash.
- Public portfolio and case study materials (HTML, slides, Figma decks): do not name CrewAI or other framework brands; use JobHunter/JobCrew or plain "AI job crew" / "agent toolkit". Dark mode only, minimal text, maximum visuals, layman's language; Figma slides must use real GUI screenshots from `docs/case-study-jobhunter/assets/gui/` (not empty placeholders), keep slide chrome as editable native Figma elements (not flattened whole-slide images), and use flat solid icons. For Figma builds load figma-use / figma-generate-design skills; derive Ajna-style tokens from the Blue Tees Golf portfolio presentation file when matching existing deck chrome.

## Learned Workspace Facts

- JobHunter AI / JobCrew is a local CrewAI job-application pipeline; public template branded JobCrew; phased build spec lives in `.planning/MASTER-PLAN.md` (v2) with session position in `.planning/STATE.md` and open items in `.planning/DEFERRED.md`.
- JobCrew role packs live under `profiles/` (Product Designer example); active profile in gitignored `user/profile.json` or `JOBCREW_PROFILE` preset; `src/jobhunter_ai/profile.py` with `GET/POST /api/profile`; resume parse via `src/jobhunter_ai/resume_parse.py`: PDF primary is the AGPL Node sidecar at `tools/open-resume-parser/` (invoked from Python), with pdfplumber heuristic fallback; DOCX/TEX stay Python (python-docx, LaTeX macros); populates experience (with bullets + startDate/endDate/isCurrent), education, languages (English default), and categorized skills; static GitHub Pages onboarding in `docs/`; portfolio case study under `docs/case-study-jobhunter/` (local HTML via `_build_local.py`, GUI screenshots in `assets/gui/`, Figma GUI inject plugin in `figma-inject-plugin/` with localhost asset server on port 8765).
- Google auth is OAuth2 with a Desktop app client (`google-oauth-client.json` plus cached `google-oauth-token.json`), not a service account; personal Google accounts give service accounts zero Drive storage quota so create-file tools fail. Credential downloads may land in `Google Auth/` before project-root paths.
- Browser automation uses Playwright with a persistent context under `browser-session/`; `DRY_RUN=True` is the default until real applications are explicitly enabled (no portal submit while dry-run). When live: LinkedIn Easy Apply downloads and uploads the tailored resume PDF; External + main Apply use direct Playwright fill first (`ats_form_fill.py`, Simplify-style field map + resume upload), with Simplify extension as fallback and harvest into `user/apply_autofill.json`. ATS email-verify (Greenhouse/Workday signals) can use Gmail readonly OAuth via `GET /api/gmail/connect` and `gmail_token.json` (`gmail_verify` / `email_verify`). On LinkedIn login wall, Chrome stays open via `browser_session.wait_for_linkedin_login()` (`JH_LOGIN_WAIT_SECONDS`, default 600).
- The agents live-preview UI lives under `dashboard/` and is served locally via `python dashboard/server.py` (default http://localhost:5959). Live `/` serves `dashboard/mockup.html` (Steep design system: warm-paper light default, Dark toggle in sidebar, `jh-steep-theme` localStorage shared with `/canvas`). Legacy `/monad`, `/steep`, `/steep-dark` alias to the live mockup. Case-study archives include `v22-slate-indigo-mock`, `v23-monad-parchment`, `v24-steep-light`, `v25-steep-dark`, `v26-profile-form-pre`, `v27-browse-activity-rings`, `v28-kg-onboarding-pre`, `v29-bento-metrics`, `v30-applicant-metrics-pre` under `dashboard-archive/` (demo: `python dashboard-archive/serve.py <vNN-slug>`; notes: `VERSIONS.md`). Applicant Dashboard is a bento metrics surface (`dashboard/metrics.js`, `metrics.css`, `GET /api/metrics`). LinkedIn Lab starts the LI section plan via `POST /api/run` and surfaces `/api/linkedin/review`. Individual Knowledge Graph uses a structured store (`dashboard/kg` defaults, `user/kg` after clone) with `stated`|`inferred` provenance via `kg_store` (v2 targets/role_stats/compensation/insights/onboarding persona+urgency/market_pulse); curated USA bands in `dashboard/kg/salary_bands.us.json`; O*NET occupation bundle under `dashboard/kg/onet/` (`occupations.json`, AI-exposure, RIASEC) drives Primary/Secondary fit scoring in `kg_store` (built via `scripts/build_onet_bundle.py` from gitignored `.onet_raw/`); Chroma/vectors are optional later for doc Q&A only, not the graph source of truth. A real KG pipeline is acquire → extract entities/relations → resolve → store → query/viz; UI visualization alone is incomplete. Browse: hover subtly elevates the job card (small scale, extra side margins, no sidebar overlap); click opens a floating JD preview sticky to the top of the list window (Steep CTA hierarchy; section label is Description not Inside; JD HTML as readable typography); dark mode listing pane continues the header background; card skeletons use inverted 20% grey. Workplace distribution uses compact concentric activity rings (Remote/Hybrid/Onsite via `#browsePieMini` / `renderWorkplaceRings`) in the search header; a scrollable expandable API-calls log between Search and the rings has Calls and Errors tabs (API failures on Errors, with Fix that retries recoverable sources and shows a clear message when unfixable); Flowbite-style card skeletons run during `loadJobs`. Browse job cards: logo from listing API then Clearbit then white medium initials on the hue square; company name in serif before/above the title; humanized dates; source chips use soft design-token tint fills with dark text (workplace chips stay quieter); UI copy floor 14px with inline icons sized to the text line; filter dropdowns must stack above surrounding content. If `/api/jobs` is empty or fails, the mockup uses labeled demo stubs (short JD). Profile resume preview is a collapsible original-file viewer (PDFs must render inline).
- Settings UI (sidebar) is open-source safe: users bring their own keys. `GET/POST /api/settings` (`src/jobhunter_ai/app_settings.py`) returns masked key status only (`set` / `missing`), upserts allowlisted keys into gitignored `.env`, and stores non-secret prefs in gitignored `user/settings.json`. Never commit `.env` or secrets; clone + paste keys locally (see `.env.example`).
- Durable error bus: `dashboard/errors/latest.json` (current opens/healed/resolved) + `dashboard/errors/history.jsonl`. Sim auto-heals safe graph issues; live failures (`awaiting_retry`, run failed) mirror into the same file. APIs: `GET/POST /api/errors/latest|report|resolve`. AutoFix (`src/jobhunter_ai/auto_fix.py`, `jh-autofix` in `dashboard/server.py`) is always on while JobHunter is in use: polls the error bus, deterministic heal + Gemini Flash allowlisted patches, at most one auto-retry with backoff before waiting for user Resume (respects `user_paused`); topbar status pill via `GET/POST /api/autofix`; no canvas pipeline card.
- Canvas graph drives execution: `buildRunPlan()` in `dashboard/app.js` compiles in-loop nodes (reachable from Trigger, or roots if none); `buildRunPlan({ sectionId })` scopes runs to a section. Figma-like `kind: "section"` frames persist in localStorage (`sections` on `jh-canvas-graph-v1`) with movable/resizable bounds; section run controls live on a right-edge floating card. Sim and live Start POST the plan; `src/jobhunter_ai/graph_crew.py` builds the sequential Crew. Trigger schedules via `POST /api/schedule` while `dashboard/server.py` is up (not OS Task Scheduler). Live Pause/Resume uses `user_paused` on `run_control.json` via `events_bus` so paused runs do not call LLMs until Play.
- Pipeline canvas cards come from `dashboard/pipeline-data.js` (generated by `dashboard/_gen_pipeline_data.py` from agents.yaml/tasks.yaml), not from static HTML. Main loop uses `AGENTS`/`EDGES`; LinkedIn loop uses `LI_AGENTS`/`LI_EDGES`/`LI_SECTION` as a separate disconnected chain (main Apply skips linkedin.com URLs). New agents must be added to `_gen_pipeline_data.py` ORDER and regenerated so they appear as cards.
- Browse and Scout share `jobhunter_ai.job_feed.fetch_job_feed` (same `job_sources` provider list, including Greenhouse/Lever/Ashby watchlist plus open APIs: RemoteOK, Remotive, Jobicy, Freehire, Rise, Arbeitnow, Himalayas, The Muse, 4 Day Week, GitHub hiring issues, Hacker News Algolia, and SerpAPI Google Jobs when `SERPAPI_API_KEY` is set). Query comes from `role_profile.search_terms` when omitted. Browse is `GET /api/jobs` (`ats_jobs.py`); Scout's tool calls the same feed. Config in `user/job_sources.json` (defaults `dashboard/job_sources.default.json`) via Profile Job sources + `GET/POST /api/job-sources`. **Scan for more open sources** (`POST /api/job-sources/scan`) probes ATS boards, live open APIs, GitHub repos, and public-apis Jobs links, then merges into watchlist / free targets / enabled API filter. Browse API filter is built from the catalog dynamically. LinkedIn excluded from Browse/Apply. `pipeline_sync` upserts Scout `scrape_and_filter_job_listings` into `job` (description included; no application row until Score); `_identity()` runs `normalize_work_mode` to `remote`|`hybrid`|`onsite`|`''`. List + preview must keep full sanitized JD text (no short clip); map Ashby/Lever/open-API description fields and hydrate Greenhouse via job JSON; `GET /api/jobs/detail` is the click-to-preview backup. Mockup `normalizeJob` merges `desc` / `description` / `content`.
- Live run telemetry flows through `src/jobhunter_ai/events_bus.py` (task/llm/tool events) into the dashboard Activity, Tokens, traces, and card Running/Thinking states. Playwright UI actions can surface via `src/jobhunter_ai/browser_preview.py` into observational Preview cards.
- Mockup Ask Cursor is the collapsible right rail (`#jhAssistantPanel` / `#jhAssistantToggle`) using `/api/cursor-chat` and `user/cursor_chat/` to reach the Cursor IDE agent (not Gemini); attachments (including image/video refs) under `user/cursor_chat/attachments/`; agents reply with `python -m jobhunter_ai.cursor_chat_reply`. Auto-scan is hook-driven (`.cursor/hooks.json`: `sessionStart`, `beforeSubmitPrompt`, `stop`, `subagentStop`) plus dashboard idle poll/nudge (`JH_CURSOR_POLL_SEC`, Ping Cursor / `jh-cursor-watch`); cold idle cannot start a Cursor agent turn by itself. Canvas bottom-left `#chatDock` remains Gemini Flash via `POST /api/chat` (`src/jobhunter_ai/canvas_chat.py`): rich project context each turn, structured `{reply, actions}`, server executes retry/abort/resolve_errors/autofix_*, client applies sim/start/stop/pause and related canvas mutations.
